{
  "name": "inventory",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/inventory/src",
  "projectType": "library",
  "tags": ["scope:inventory"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/inventory",
        "main": "libs/inventory/src/index.ts",
        "tsConfig": "libs/inventory/tsconfig.lib.json",
        "assets": []
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/inventory/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/inventory"],
      "options": {
        "jestConfig": "libs/inventory/jest.config.ts",
        "passWithNoTests": true
      }
    }
  }
}
